{% extends 'base.html.twig' %}

{% block title %}Basket{% endblock %}

{% block body %}
    <h1>Basket</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ basket.id }}</td>
            </tr>
            <tr>
                <th>LastUpdate</th>
                <td>{{ basket.lastUpdate ? basket.lastUpdate|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_basket_admin_index') }}">back to list</a>

    <a href="{{ path('app_basket_admin_edit', {'id': basket.id}) }}">edit</a>

    {{ include('basket_admin/_delete_form.html.twig') }}
{% endblock %}
