<form method="post" action="{{ path('app_card_delete', {'id': card.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce produit ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ card.id) }}">
    <button class="btn text-blue">Supprimer</button>
</form>
